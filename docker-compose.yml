version: '3'

services:
  app:
    image: saiki987/tcg-simulator:1.1.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    # command: bash -c ". ~/.nvm/nvm.sh && yarn start --port ${PORT}"
    environment:
     - "PORT=${PORT}"
